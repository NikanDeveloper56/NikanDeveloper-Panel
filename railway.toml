[build]
builder = "dockerfile"
dockerfile = "Dockerfile"

[deploy]
startCommand = "/app/x-ui"
healthcheckPath = "/login"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
